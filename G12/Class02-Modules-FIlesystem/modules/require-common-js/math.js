const addNumbers = (numOne, numTwo) => numOne + numTwo;
const multiplyNumbers = (numOne, numTwo) => numOne * numTwo;

// Only one module.exports per module file, export anything you need here
module.exports = {
  addNumbers,
  multiplyNumbers,
};
